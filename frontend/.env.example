VITE_API_URL=http://localhost:4000

# --- Suivi des erreurs (Sentry) ---
# DSN du projet Sentry (frontend — distinct de celui du backend). Laissez
# vide pour désactiver Sentry entièrement (aucune donnée envoyée) — utile en
# local/dev.
VITE_SENTRY_DSN=
